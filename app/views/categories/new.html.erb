<%= simple_form_for @category do |f| %>
    <%= f.input :name %>
    <%= f.association :parent_category, collection: @available_categories %>
    <%= f.button :submit %>
<% end %>

<%=  link_to 'Cancel', categories_path %>
